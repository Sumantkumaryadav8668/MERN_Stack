// Asign the element one array to another array


    // If first array of element are asign the second array 
    // than any operation are applied than change the first array
let arr = [50,40,60,true]
let arr1 = arr
arr1.push(100)

console.log(arr1);