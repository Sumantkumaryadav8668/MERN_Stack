 // Print Key in object 

let user ={
    name:'kumar sumant',
    age:22,
    amount:20000,
}
            // Print Keys 
console.log(Object.keys(user));

            // Print values 
console.log(Object.values(user));

           // Print entries 
console.log(Object.entries(user));